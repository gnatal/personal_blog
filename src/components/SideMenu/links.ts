const about = {
  linkURL: '/about',
  linkLabel: 'sobre',
};

const login = {
  linkURL: '/login',
  linkLabel: 'login',
};

const home = {
  linkURL: '/',
  linkLabel: 'início',
};

const posts = {
  linkURL: '/posts',
  linkLabel: 'blog',
};

export default {
  about,
  posts,
  login,
  home,
};
